<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Students Dashboard</title>
    <link rel="stylesheet" href="css/home-styles.css" type="text/css" />
  </head>
  <body>
    To register click <a href="register">here</a>.
    <h1>List of users</h1>

    <% if(!data.length){ %>
    <p>No students have registered yet !</p>
    <%}else{%>
    <table border="1" cellpadding="10">
      <tr>
        <th>First Name</th>
        <th>Last Name</th>
        <th>Email</th>
        <th>Phone Number</th>
        <th>Action</th>
      </tr>
      <%data.reverse().forEach((d) => {%>
      <tr>
        <td><%= d.fname%></td>
        <td><%= d.lname%></td>
        <td><%= d.email%></td>
        <td><%= d.phone%></td>
        <td>
          <a href="details/<%=d.created_at%>">View Details</a>
        </td>
      </tr>
      <%})%>
    </table>
    <%}%>
  </body>
</html>

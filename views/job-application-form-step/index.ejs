<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Job Application Form</title>
    <style>
      body > * {
        margin: 0 auto;
        width: fit-content;
      }

      h3 {
        margin: 20px auto;
      }

      fieldset {
        background-color: lavender;
        width: fit-content;
      }

      fieldset input:not([disabled]),
      textarea,
      select:not([disabled]) {
        color: hsla(227.8, 96%, 29.2%, 0.88);
      }

      fieldset > * {
        background-color: gainsboro;
      }

      legend {
        color: purple;
        font-size: large;
        font-weight: normal;
        font-family: sans-serif;
      }

      .navigation {
        display: flex;
        justify-content: end;
      }

      .prev-btn {
        padding: 10px;
        margin: 10px;
        border: 2px solid black;
        border-radius: 10px;
        cursor: pointer;
        font-weight: bolder;
        font-family: sans-serif;
        -webkit-user-select: none; /* Safari */
        -ms-user-select: none; /* IE 10 and IE 11 */
        user-select: none; /* Standard syntax */
      }

      .next-btn {
        padding: 10px;
        margin: 10px;
        border: 2px solid black;
        background-color: darkgreen;
        color: white;
        border-radius: 10px;
        cursor: pointer;
        font-weight: bolder;
        font-family: sans-serif;
        -webkit-user-select: none; /* Safari */
        -ms-user-select: none; /* IE 10 and IE 11 */
        user-select: none; /* Standard syntax */
      }

      .on ~ div {
        color: red;
        font-size: small;
      }

      .addExp {
        margin: 20px 10px 20px auto;
        width: auto;
        display: flex;
        justify-content: end;
        background-color: inherit;
      }
    </style>
  </head>
  <body>
    <input
      type="hidden"
      name="oldState"
      id="oldState"
      value="<%=JSON.stringify(data)%>"
    />
    <h1>Job Application Form</h1>
    <h3>Aryaman Karde DEV072</h3>
    <div id="root"></div>
  </body>
  <script
    src="/job-application-form-step/js/index.js"
    type="text/javascript"
  ></script>
</html>
